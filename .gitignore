build/
target/
.gradle/
.kotlin/
.idea/
*.hprof
local.properties

# Runtime DB files. kotlin-rest creates and seeds its own on first run, so
# nothing under data/ belongs in the repository.
data/

# Frontend (node/vite/TS)
node_modules/
dist/
*.tsbuildinfo
vite.config.d.ts
vite.config.js
.env
